# Build output
/bin/

# Host assets and runtime state (never committed — machine-specific, can be large)
/opt/
*.ext4
*.sock
vmlinux
registry.json

# Go
/vendor/
# NOTE: go.sum is intentionally committed (dependency integrity).

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
